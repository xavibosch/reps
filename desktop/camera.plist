<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- macOS refuses to hand over the camera to a bundle that does not declare
	     why it wants it: without this key the app is killed on the first
	     getUserMedia call rather than prompting. This string is what the system
	     prompt shows, once, on first launch. -->
	<key>NSCameraUsageDescription</key>
	<string>Reps uses the camera to track your hand so you can log sets without touching the keyboard.</string>
</dict>
</plist>
